define(['interface'], (__interface__) ->
    residenceService = () ->
        serviceInterface = {}
        serviceInterface

    __interface__.service('residence', [
        residenceService
    ])
)
